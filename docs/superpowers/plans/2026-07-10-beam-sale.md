# Moduł sprzedaży dwuteowników (SH) — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Czwarty pod-moduł trybu SPRZEDAŻ — sprzedaż dwuteowników HEB/HEA/IPE z numeracją SH/YYYY/NNN, cennikiem 1-wierszowym (domyślna sprzedaż 3800 PLN/t + domyślny koszt), pełnym cyklem zapis/edycja/kopia/PDF PL-EN i flagą „dwuteowniki używane".

**Architecture:** Izolowany klon w `src/components/sale/beam/` — dane i wzór masy z wynajmu belek (`beam_profiles`, masa = szt × L × kg/m / 1000), warstwa handlowa ze sprzedaży grodzic (`SaleCalculator`/`SaveSaleOfferModal`), kontener i saga edycji z SPP. Nowe tabele `beam_sale_*`, wspólny tylko katalog `beam_profiles` + `clients`.

**Tech Stack:** React 18 + TypeScript + Vite 5, Tailwind 3, Supabase (PostgreSQL, projekt `hliemaqfncptedkxxakt`), `@react-pdf/renderer`.

**Spec:** `docs/superpowers/specs/2026-07-10-beam-sale-design.md`

**Weryfikacja w tym repo:** brak test runnera — „test" = `npm --prefix grodzice-kalkulator run build` (strict tsc, uruchamiany z `C:\Users\doman\CENNIK`; sukces = `✓ built in`) + weryfikacja behawioralna w przeglądarce (Task 12). Commit po każdym tasku (styl użytkownika).

---

## File Structure

**Nowe pliki:**
- `docs/migrations/2026-07-10-beam-sale.sql` — migracja DB (4 tabele + 2 triggery + seed)
- `src/components/sale/beam/BeamSaleSection.tsx` — kontener (4 zakładki)
- `src/components/sale/beam/BeamSaleCalculator.tsx` — kalkulator
- `src/components/sale/beam/BeamSaveSaleOfferModal.tsx` — modal zapisu (eksportuje `BeamSaleItemSnapshot`)
- `src/components/sale/beam/BeamSaleOffersTable.tsx` — lista ofert SH
- `src/components/sale/beam/BeamSaleEditOfferModal.tsx` — edycja + kopia (saga)
- `src/components/sale/beam/BeamSalePriceSettings.tsx` — zakładka Cennik
- `src/components/sale/beam/BeamSaleOfferPDF.tsx` — PDF PL/EN

**Modyfikowane:**
- `src/types/index.ts` — dopisz `BeamSalePrices`, `BeamSaleOffer`, `BeamSaleOfferItem` (po bloku Beam rental, ~linia 217)
- `src/lib/pdfStrings.ts` — dopisz interfejs `BeamSalePdfStrings` + `beamSale_pl` + `beamSale_en` + eksport `BEAM_SALE_PDF_STRINGS` (na końcu pliku)
- `src/App.tsx` — `SaleSubMode` + `'beam'`, stany, `beamSaleTabs`, przycisk sub-toggle, render

**Pliki-wzorce (źródło prawdy do skopiowania, NIE modyfikować):**
- `src/components/beam/BeamCalculator.tsx` — pozycje (profil/gatunek/szt/dł/masa), linie 18–24 (CalcItem), 127–150 (itemResults/totals)
- `src/components/sale/SaleCalculator.tsx` — warstwa handlowa: marginColor/Label (59–71), handleCurrencyChange (397–411), dostawa 4 kafelki (1112–1254), kafelek „używane" (1257–1275), applyPriceToAll (245–248)
- `src/components/sale/SaveSaleOfferModal.tsx` — struktura modala zapisu (całość), warunki oferty (530–721)
- `src/components/sale/roadplate/RoadPlateSaleSection.tsx` — kontener
- `src/components/sale/roadplate/RoadPlateSaleOffersTable.tsx` — tabela ofert (bez zamków)
- `src/components/sale/roadplate/RoadPlateEditOfferModal.tsx` — saga edycji + mode copy
- `src/components/beam/BeamPriceSettings.tsx` — zakładka cennika (struktura form/toast/save)
- `src/components/beam/BeamOfferPDF.tsx` — geometria PDF, style, meta, transport box
- `src/components/sale/SaleOfferPDF.tsx` — price box (504–544), deliveryTimelineText/deliveryTermsText (237–257), sekcje termin/warunki dostawy (671–685), wariant isUsed (687–703)
- `src/lib/pdfStrings.ts` — `BEAM_RENTAL_PDF_STRINGS` (interfejs ~1103, _pl ~1198, _en ~1290) i `PdfStrings` sprzedaży grodzic (~116, _pl ~220, _en ~326)
- `docs/migrations/2026-07-07-beam-rental.sql` — wzorzec migracji

---

## Task 1: Migracja DB — tabele `beam_sale_*`

**Files:**
- Create: `docs/migrations/2026-07-10-beam-sale.sql`

- [ ] **Step 1: Napisz plik migracji** — pełna treść:

```sql
-- ============================================================================
-- Migration: Beam Sale (dwuteowniki HEB/HEA/IPE — sprzedaż, prefiks SH)
-- Data: 2026-07-10
-- Strategia: 100% addytywna. Wspólny katalog beam_profiles (bez zmian).
-- Spec: docs/superpowers/specs/2026-07-10-beam-sale-design.md
-- RLS celowo NIE włączamy (konwencja wszystkich tabel ofert w projekcie).
-- ============================================================================

-- ─── 1. Sekwencja numeracji SH (izolowana) ────────────────────────────────────
CREATE TABLE IF NOT EXISTS beam_sale_sequences (
  year          INTEGER     PRIMARY KEY,
  last_sequence INTEGER     NOT NULL DEFAULT 0,
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
COMMENT ON TABLE beam_sale_sequences IS
  'Sekwencja numerów ofert sprzedaży dwuteowników (SH/YYYY/NNN). Izolowana od OF/OP/OH/SP/SR/SPP.';

-- ─── 2. Cennik sprzedaży belek (1 wiersz) ─────────────────────────────────────
CREATE TABLE IF NOT EXISTS beam_sale_prices (
  id                              UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  default_sell_price_per_ton_pln  NUMERIC     NOT NULL DEFAULT 0,
  default_cost_price_per_ton_pln  NUMERIC     NOT NULL DEFAULT 0,
  note                            TEXT,
  updated_at                      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
COMMENT ON TABLE beam_sale_prices IS
  'Cennik sprzedaży dwuteowników (1 wiersz). Ceny kanoniczne w PLN; kalkulator pre-filluje pozycje.';

INSERT INTO beam_sale_prices (default_sell_price_per_ton_pln, default_cost_price_per_ton_pln, note)
SELECT 3800, 0, 'Domyślna cena sprzedaży 3800 PLN/t (ustawiona 2026-07-10). Koszt 0 = brak pre-fill.'
WHERE NOT EXISTS (SELECT 1 FROM beam_sale_prices);

-- ─── 3. Nagłówek oferty sprzedaży belek ───────────────────────────────────────
CREATE TABLE IF NOT EXISTS beam_sale_offers (
  id                        UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  offer_number              TEXT        NOT NULL DEFAULT '' UNIQUE,
  year                      INTEGER     NOT NULL DEFAULT 0,
  sequence                  INTEGER     NOT NULL DEFAULT 0,
  client_id                 UUID        REFERENCES clients(id) ON DELETE SET NULL,
  task_name                 TEXT,
  is_used                   BOOLEAN     NOT NULL DEFAULT FALSE,
  status                    TEXT        NOT NULL DEFAULT 'szkic'
    CHECK (status IN ('szkic','wysłana','przyjęta','odrzucona')),
  notes                     TEXT,
  valid_days                INTEGER     NOT NULL DEFAULT 14,
  payment_days              INTEGER     NOT NULL DEFAULT 30,
  prepared_by               TEXT,
  currency                  TEXT        NOT NULL DEFAULT 'PLN'
    CHECK (currency IN ('EUR','PLN')),
  exchange_rate             NUMERIC,
  -- Sumy (snapshot)
  total_mass_t              NUMERIC,
  total_cost_eur            NUMERIC,
  total_sell_eur            NUMERIC,
  total_sell_pln            NUMERIC,
  margin_pct                NUMERIC,
  -- Dostawa: koszty
  delivery_trucks           NUMERIC,
  delivery_cost_per_truck   NUMERIC,
  delivery_cost_total       NUMERIC,
  delivery_paid_by          TEXT
    CHECK (delivery_paid_by IS NULL OR delivery_paid_by IN ('dap_included','dap_extra','fca','cif')),
  delivery_from             TEXT,
  delivery_to               TEXT,
  -- Warunki oferty
  delivery_timeline         TEXT
    CHECK (delivery_timeline IS NULL OR delivery_timeline IN ('huta','magazyn')),
  campaign_weeks            TEXT,
  campaign_delivery_weeks   TEXT,
  warehouse_delivery_time   TEXT,
  delivery_terms            TEXT
    CHECK (delivery_terms IS NULL OR delivery_terms IN ('DAP','DAP_EXTRA','FCA','CIF')),
  fca_location              TEXT,
  -- Audit / soft-delete
  created_at                TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at                TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at                TIMESTAMPTZ
);
COMMENT ON TABLE beam_sale_offers IS
  'Oferty sprzedaży dwuteowników (SH/YYYY/NNN). Soft-delete przez deleted_at.';

CREATE INDEX IF NOT EXISTS idx_beam_sale_offers_deleted  ON beam_sale_offers(deleted_at);
CREATE INDEX IF NOT EXISTS idx_beam_sale_offers_created  ON beam_sale_offers(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_beam_sale_offers_client   ON beam_sale_offers(client_id);
CREATE INDEX IF NOT EXISTS idx_beam_sale_offers_year_seq ON beam_sale_offers(year, sequence);

-- ─── 4. Pozycje oferty sprzedaży belek ────────────────────────────────────────
-- cost_per_ton/sell_per_ton/cost_total/sell_total: W WALUCIE OFERTY.
-- sell_value_eur/sell_value_pln: denominacje zawsze wyliczone.
-- steel_grade CHECK ↔ BEAM_STEEL_GRADES w types/index.ts (utrzymuj zgodne).
CREATE TABLE IF NOT EXISTS beam_sale_offer_items (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  offer_id         UUID        NOT NULL REFERENCES beam_sale_offers(id) ON DELETE CASCADE,
  profile_id       UUID        REFERENCES beam_profiles(id) ON DELETE SET NULL,
  profile_name     TEXT        NOT NULL,
  series           TEXT        NOT NULL,
  weight_kg_per_m  NUMERIC     NOT NULL CHECK (weight_kg_per_m > 0),
  steel_grade      TEXT        NOT NULL
    CHECK (steel_grade IN ('S235','S275','S355','min. S235','min. S275','min. S355')),
  quantity_pcs     INTEGER     NOT NULL CHECK (quantity_pcs > 0),
  length_m         NUMERIC     NOT NULL CHECK (length_m > 0),
  total_length_m   NUMERIC     NOT NULL CHECK (total_length_m > 0),
  mass_t           NUMERIC     NOT NULL CHECK (mass_t > 0),
  cost_per_ton     NUMERIC     NOT NULL DEFAULT 0 CHECK (cost_per_ton >= 0),
  sell_per_ton     NUMERIC     NOT NULL CHECK (sell_per_ton >= 0),
  cost_total       NUMERIC     NOT NULL DEFAULT 0 CHECK (cost_total >= 0),
  sell_total       NUMERIC     NOT NULL CHECK (sell_total >= 0),
  sell_value_eur   NUMERIC     NOT NULL CHECK (sell_value_eur >= 0),
  sell_value_pln   NUMERIC     NOT NULL CHECK (sell_value_pln >= 0),
  margin_pct       NUMERIC,
  sort_order       INTEGER     NOT NULL DEFAULT 0,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
COMMENT ON TABLE beam_sale_offer_items IS
  'Pozycje ofert sprzedaży belek. ON DELETE CASCADE na offer_id. profile_id ON DELETE SET NULL.';

CREATE INDEX IF NOT EXISTS idx_beam_sale_items_offer   ON beam_sale_offer_items(offer_id);
CREATE INDEX IF NOT EXISTS idx_beam_sale_items_profile ON beam_sale_offer_items(profile_id);

-- ─── 5. Trigger numeracji SH/YYYY/NNN ─────────────────────────────────────────
CREATE OR REPLACE FUNCTION generate_beam_sale_offer_number()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_year     INTEGER;
  v_sequence INTEGER;
BEGIN
  v_year := EXTRACT(YEAR FROM timezone('Europe/Warsaw', NOW()))::INTEGER;

  INSERT INTO beam_sale_sequences (year, last_sequence, updated_at)
  VALUES (v_year, 1, NOW())
  ON CONFLICT (year)
    DO UPDATE SET
      last_sequence = beam_sale_sequences.last_sequence + 1,
      updated_at    = NOW()
  RETURNING last_sequence INTO v_sequence;

  NEW.year         := v_year;
  NEW.sequence     := v_sequence;
  NEW.offer_number := 'SH/' || v_year || '/' || LPAD(v_sequence::TEXT, 3, '0');

  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_beam_sale_offer_number ON beam_sale_offers;
CREATE TRIGGER trg_beam_sale_offer_number
  BEFORE INSERT ON beam_sale_offers
  FOR EACH ROW
  EXECUTE FUNCTION generate_beam_sale_offer_number();

-- ─── 6. Triggery touch updated_at (reuse funkcji z migracji beam-rental) ──────
DROP TRIGGER IF EXISTS trg_beam_sale_offer_touch ON beam_sale_offers;
CREATE TRIGGER trg_beam_sale_offer_touch
  BEFORE UPDATE ON beam_sale_offers
  FOR EACH ROW
  EXECUTE FUNCTION touch_beam_updated_at();

DROP TRIGGER IF EXISTS trg_beam_sale_prices_touch ON beam_sale_prices;
CREATE TRIGGER trg_beam_sale_prices_touch
  BEFORE UPDATE ON beam_sale_prices
  FOR EACH ROW
  EXECUTE FUNCTION touch_beam_updated_at();
```

- [ ] **Step 2: Zastosuj migrację w Supabase**

Przez MCP: `apply_migration` (projekt `hliemaqfncptedkxxakt`, name `beam_sale`), treść = plik z Step 1. Jeśli MCP rozłączony — poinstruuj użytkownika, by wkleił plik do SQL Editor.

- [ ] **Step 3: Weryfikacja**

`execute_sql`:
```sql
SELECT proname, prosecdef FROM pg_proc WHERE proname = 'generate_beam_sale_offer_number';
-- oczekiwane: prosecdef = true
SELECT default_sell_price_per_ton_pln FROM beam_sale_prices;
-- oczekiwane: 3800 (1 wiersz)
BEGIN;
  INSERT INTO beam_sale_offers (status, currency) VALUES ('szkic','PLN')
  RETURNING offer_number;
ROLLBACK;
-- oczekiwane: SH/2026/001; ROLLBACK nie zostawia oferty (sekwencja może się przesunąć — akceptowalne, expected PG behavior)
SELECT tablename FROM pg_tables WHERE tablename LIKE 'beam_sale%' ORDER BY tablename;
-- oczekiwane: beam_sale_offer_items, beam_sale_offers, beam_sale_prices, beam_sale_sequences
```

- [ ] **Step 4: Commit**

```bash
git add docs/migrations/2026-07-10-beam-sale.sql
git commit -m "feat(beam-sale): migracja DB — tabele beam_sale_* + trigger SH (Task 1)"
```

---

## Task 2: Typy TypeScript

**Files:**
- Modify: `src/types/index.ts` (wstaw po `BeamRentalOfferItem`, przed sekcją `// ─── Sprzedaż ───`, ~linia 217)

- [ ] **Step 1: Dodaj trzy interfejsy** — pełny kod:

```ts
// ─── Sprzedaż dwuteowników (SH) ──────────────────────────────────────────────

export interface BeamSalePrices {
  id: string;
  default_sell_price_per_ton_pln: number;  // pre-fill ceny sprzedaży (PLN kanoniczne)
  default_cost_price_per_ton_pln: number;  // pre-fill ceny kosztu (0 = brak pre-fill)
  note?: string | null;
  updated_at: string;
}

export interface BeamSaleOffer {
  id: string;
  offer_number: string;                  // SH/YYYY/NNN
  year: number;
  sequence: number;
  client_id?: string;
  client?: Client;
  task_name?: string;
  is_used?: boolean;                     // true = dwuteowniki używane (skrócona sekcja techniczna PDF)
  status: OfferStatus;
  notes?: string;
  valid_days: number;
  payment_days: number;
  prepared_by?: string;
  currency: 'EUR' | 'PLN';
  exchange_rate?: number;
  // Sumy snapshot
  total_mass_t?: number;
  total_cost_eur?: number;
  total_sell_eur?: number;
  total_sell_pln?: number;
  margin_pct?: number;
  // Dostawa: koszty
  delivery_trucks?: number;
  delivery_cost_per_truck?: number;
  delivery_cost_total?: number;          // zawsze PLN (konwencja SP)
  delivery_paid_by?: 'dap_included' | 'dap_extra' | 'fca' | 'cif';
  delivery_from?: string;
  delivery_to?: string;
  // Warunki oferty
  delivery_timeline?: 'huta' | 'magazyn';
  campaign_weeks?: string;
  campaign_delivery_weeks?: string;
  warehouse_delivery_time?: string;
  delivery_terms?: 'DAP' | 'DAP_EXTRA' | 'FCA' | 'CIF';
  fca_location?: string;
  // Audit / soft-delete
  created_at: string;
  updated_at: string;
  deleted_at?: string | null;
  items?: BeamSaleOfferItem[];
}

export interface BeamSaleOfferItem {
  id: string;
  offer_id: string;
  profile_id?: string | null;            // FK ON DELETE SET NULL
  // Snapshot atrybutów profilu
  profile_name: string;
  series: string;
  weight_kg_per_m: number;
  steel_grade: BeamSteelGrade;
  // Ilość i agregaty
  quantity_pcs: number;
  length_m: number;
  total_length_m: number;
  mass_t: number;                        // total_length × kg/m / 1000, 3dp
  // Ceny (konwencja SP: wartości w WALUCIE OFERTY mimo braku sufiksu)
  cost_per_ton: number;
  sell_per_ton: number;
  cost_total: number;
  sell_total: number;
  // Denominacje (zawsze wyliczone)
  sell_value_eur: number;
  sell_value_pln: number;
  margin_pct?: number;
  sort_order: number;
  created_at?: string;
}
```

- [ ] **Step 2: Build**

Run: `npm --prefix grodzice-kalkulator run build` (z `C:\Users\doman\CENNIK`)
Expected: `✓ built in` (PowerShell może owinąć ostrzeżenie chunk-size w NativeCommandError — to sukces).

- [ ] **Step 3: Commit**

```bash
git add src/types/index.ts
git commit -m "feat(beam-sale): typy BeamSalePrices/BeamSaleOffer/BeamSaleOfferItem (Task 2)"
```

---

## Task 3: Stringi PDF (`pdfStrings.ts`)

**Files:**
- Modify: `src/lib/pdfStrings.ts` (dopisz NA KOŃCU pliku — nowy blok, nie ruszaj istniejących)

- [ ] **Step 1: Dodaj interfejs `BeamSalePdfStrings`**

Struktura = klon interfejsu `BeamRentalPdfStrings` (linia ~1103) ZMODYFIKOWANY o warstwę sprzedażową z `PdfStrings` grodzic (linia ~116). Pełna lista pól:

```ts
// ─── Sprzedaż dwuteowników (SH) ──────────────────────────────────────────────
export interface BeamSalePdfStrings {
  docTitle:          (offerNumber: string) => string;
  docLanguage:       string;
  offerTitle:        string;
  date:              string;
  offerNumber:       string;
  salesRep:          string;
  phone:             string;
  customerLabel:     string;   // 'Klient:' (konwencja rur/płyt-sprzedaż)
  vatLabel:          (country: string) => string;
  taskLabel:         string;
  greeting:          string;
  intro:             string;   // tekst sprzedażowy z linkiem OWH (klon PdfStrings.intro, 'grodzic' → 'dwuteowników')
  // Tabela pozycji (7 kolumn)
  thProfile:         string;
  thSteelGrade:      string;
  thQty:             string;
  thLength:          string;
  thMass:            string;
  thPricePerT:       string;   // 'Cena [EUR/t]' — nagłówek nadpisywany dla PLN w komponencie
  thValue:           string;   // 'Wartość [EUR]'
  unitPcs:           string;
  totalRow:          string;
  // Cena box
  priceLabel:        string;   // 'CENA CAŁKOWITA (NETTO)'
  netSuffix:         string;
  breakdownBeams:    string;   // 'Dwuteowniki' / 'Steel beams' (rozbicie w price box)
  breakdownDelivery: string;   // 'Dostawa' / 'Delivery'
  // Transport (klon BEAM_RENTAL_PDF_STRINGS)
  sectionTransport:  string;
  labelDelivery:     string;
  valueDapIncluded:  string;
  valueDapExtra:     string;
  valueFca:          string;
  valueCif:          string;
  labelRoute:        string;
  labelTrucks:       string;
  labelCostPerTruck: string;
  labelTotalTransport: string;
  labelSettlement:   string;
  valueRecharge:     string;
  labelPickupFrom:   string;
  // Termin dostawy (klon PdfStrings sprzedaży grodzic)
  sectionDeliveryTime: string;
  deliveryFromMill:  (weeks: string, deliveryWeeks?: string) => string;
  deliveryFromStock: (time: string) => string;
  // Warunki dostawy Incoterms (klon PdfStrings)
  sectionDeliveryTerms: string;
  deliveryDap:       (destination: string) => string;
  deliveryDapExtra:  (destination: string) => string;
  deliveryFca:       (location: string) => string;
  deliveryCif:       (port: string) => string;
  // Warunki techniczne (NOWE brzmienia — patrz Step 2)
  sectionTechnical:  string;
  techStandard:      string;
  techGrade:         string;
  techTolerance:     string;
  techCert:          string;
  techWeighing:      string;
  techUsedLabel:     string;
  techUsedNote:      string;
  // Płatność / ważność / notatki (klon PdfStrings)
  sectionPayment:    string;
  paymentPrepaid:    string;
  paymentCredit:     (days: number) => string;
  sectionValidity:   string;
  validityLabel:     (days: number) => string;
  validityLine:      (label: string) => string;
  validityDisclaimer: string;
  notesLabel:        string;
}
```

- [ ] **Step 2: Dodaj `beamSale_pl`** — wartości: klonuj odpowiadające pola z `pdf_pl` (PdfStrings sprzedaży grodzic) i `beamRental_pl`, zamieniając „grodzice/grodzic" → „dwuteowniki/dwuteowników". Pola NOWE lub zmienione (dokładne brzmienia):

```ts
const beamSale_pl: BeamSalePdfStrings = {
  // ... klony z pdf_pl / beamRental_pl ...
  offerTitle:      'OFERTA SPRZEDAŻY DWUTEOWNIKÓW',
  customerLabel:   'Klient:',
  thPricePerT:     'Cena [EUR/t]',
  thValue:         'Wartość [EUR]',
  breakdownBeams:  'Dwuteowniki',
  breakdownDelivery: 'Dostawa',
  // Warunki techniczne — zamówione przez użytkownika:
  techStandard:    '- produkowane i dostarczane zgodnie z normą EN 10034.',
  techGrade:       '- gatunek stali zgodny z ofertą.',
  techTolerance:   '- tolerancja długości -50/+50 mm.',
  techCert:        '- certyfikat 3.1 zgodnie z normą EN10204.',
  techWeighing:    '- fakturowanie wg. wagi teoretycznej.',
  techUsedLabel:   'Dwuteowniki używane:',
  techUsedNote:    'Dwuteowniki używane mogą posiadać ślady użytkowania, w tym zabrudzenia, przylegającą ziemię, rdzę, pozostałości powłok ochronnych, otwory technologiczne, ślady spawania i cięcia oraz uszkodzenia mechaniczne i odchyłki wymiarowe wynikające z eksploatacji.',
};
```

- [ ] **Step 3: Dodaj `beamSale_en`** — analogicznie z `pdf_en` / `beamRental_en`. Pola NOWE:

```ts
const beamSale_en: BeamSalePdfStrings = {
  // ... klony z pdf_en / beamRental_en ...
  offerTitle:      'STEEL BEAM SALE OFFER',
  customerLabel:   'Customer:',
  breakdownBeams:  'Steel beams',
  breakdownDelivery: 'Delivery',
  techStandard:    '- produced and supplied in accordance with EN 10034.',
  techGrade:       '- steel grade as stated in this quotation.',
  techTolerance:   '- length tolerance -50/+50 mm.',
  techCert:        '- mill certificate 3.1 in accordance with EN 10204.',
  techWeighing:    '- invoicing based on theoretical weight.',
  techUsedLabel:   'Used steel beams:',
  techUsedNote:    'Used steel beams may show signs of use, including dirt, adhering soil, rust, residues of protective coatings, technological holes, welding and cutting marks, as well as mechanical damage and dimensional deviations resulting from operation.',
};

export const BEAM_SALE_PDF_STRINGS: Record<PdfLang, BeamSalePdfStrings> = {
  pl: beamSale_pl,
  en: beamSale_en,
};
```

- [ ] **Step 4: Build + weryfikacja językowa**

Run: `npm --prefix grodzice-kalkulator run build` → `✓ built in`.
Potem grep na FAKTYCZNE wartości (gotcha replace_all PL/EN z CLAUDE.md):
`Grep pattern="techStandard.*EN 10034" path=src/lib/pdfStrings.ts` → oczekiwane 2 trafienia (PL + EN, różne języki zdania). Sprawdź, że w `beamSale_en` nie został polski string (kotwica: sąsiednie pole po angielsku).

- [ ] **Step 5: Commit**

```bash
git add src/lib/pdfStrings.ts
git commit -m "feat(beam-sale): slownik PDF BeamSalePdfStrings PL/EN (Task 3)"
```

---

## Task 4: `BeamSaleOfferPDF.tsx`

**Files:**
- Create: `src/components/sale/beam/BeamSaleOfferPDF.tsx`

- [ ] **Step 1: Sklonuj szkielet z `BeamOfferPDF.tsx`** (całość: importy, SALES_REPS/SIGNATURES, Font.register, kolory C, style s, geometria paddingTop 108 / paddingBottom 130, meta+klient, greeting+intro z linkiem OWH, transport box `<View wrap={false}>`, notatki, podpis). Zmiany globalne:
  - `import type { BeamSaleOffer } from '../../../types'` (UWAGA: 3 poziomy — plik leży w `sale/beam/`)
  - `import { BEAM_SALE_PDF_STRINGS, translateWarehouseLocation, translateWarehouseDeliveryTime } from '../../../lib/pdfStrings'`
  - `const t = BEAM_SALE_PDF_STRINGS[lang]`
  - Usuń: sekcje wynajmu (okres dzierżawy, stawka tygodniowa, warunki dzierżawy, cennik szkód, para1–para4, formatRentalPeriod).

- [ ] **Step 2: Tabela pozycji — 7 kolumn SPRZEDAŻOWYCH** (⚠ inny zestaw niż rental mimo tej samej liczby — rental ma kg/m i koszt/t wspólny; tu cena i wartość per pozycja):

| Kolumna | flex | treść |
|---|---|---|
| Profil | 2.2 | `item.profile_name` (bold) |
| Gatunek | 1.4 | `item.steel_grade` |
| Ilość | 1.0 | `{item.quantity_pcs} {t.unitPcs}` (center) |
| Długość | 1.1 | `{item.length_m} m` (right) |
| Masa | 1.2 | `formatNumber(item.mass_t, 3)` t (right, bold) |
| Cena/t | 1.3 | `formatRound(item.sell_per_ton)` + waluta (right) |
| Wartość | 1.4 | `isEUR ? formatEUR(item.sell_value_eur) : formatPLN(item.sell_value_pln)` (right, bold) |

Nagłówki `thPricePerT`/`thValue` nadpisywane dla PLN jak w `SaleOfferPDF.tsx:196-198` (lokalny const `thPriceT = isEUR ? t.thPricePerT : (lang === 'pl' ? 'Cena [PLN/t]' : 'Price [PLN/t]')` itd.). Wiersz sumy: masa łączna + wartość łączna.

- [ ] **Step 3: Price box + rozbicie** — lustro `SaleOfferPDF.tsx:504-544` bez zamków:
  - `totalForClientEUR = totalSellEUR + deliveryCostEUR` (delivery tylko gdy `dap_included`; `delivery_cost_total` w DB zawsze PLN → `/exchRate` dla EUR)
  - Rozbicie: `{t.breakdownBeams}: {beamVal} · {pricePerT} /t` gdzie `beamVal = sell + delivery` przy dap_included (transport wliczony, bez osobnej linii — wzorzec SP).

- [ ] **Step 4: Sekcje warunków** — kolejność jak `SaleOfferPDF`:
  1. Transport box (klon z BeamOfferPDF — 4 warianty dPaidBy, `<View wrap={false}>`)
  2. Termin dostawy: funkcja `deliveryTimelineText()` — lustro `SaleOfferPDF.tsx:237-244` (huta → `t.deliveryFromMill`, magazyn → `t.deliveryFromStock(translateWarehouseDeliveryTime(...))`)
  3. Warunki dostawy: `deliveryTermsText()` — lustro `SaleOfferPDF.tsx:246-257` (FCA/CIF/DAP_EXTRA/DAP)
  4. Warunki techniczne z wariantem `isUsed` — lustro `SaleOfferPDF.tsx:687-703`:
     - normalne: techStandard → techGrade → techTolerance → techCert → techWeighing
     - `is_used`: bold `techUsedLabel` → techTolerance → techWeighing → techUsedNote
  5. Płatność (`paymentPrepaid`/`paymentCredit`), Ważność (`validityLine`), disclaimer, notatki, podpis.

- [ ] **Step 5: Build**

Run: `npm --prefix grodzice-kalkulator run build` → `✓ built in` (tsc wymusi kompletność użytych kluczy słownika).

- [ ] **Step 6: Commit**

```bash
git add src/components/sale/beam/BeamSaleOfferPDF.tsx
git commit -m "feat(beam-sale): PDF oferty SH — 7 kolumn, warunki EN 10034, wariant uzywane (Task 4)"
```

---

## Task 5: `BeamSalePriceSettings.tsx` (zakładka Cennik)

**Files:**
- Create: `src/components/sale/beam/BeamSalePriceSettings.tsx`

- [ ] **Step 1: Sklonuj `src/components/beam/BeamPriceSettings.tsx`** okrojony do 2 pól:
  - Props: `{ prices: BeamSalePrices; onPricesChange: (p: BeamSalePrices) => void }`
  - FormState: `default_sell_price_per_ton_pln: string; default_cost_price_per_ton_pln: string; note: string`
  - Walidacja: sprzedaż > 0; koszt ≥ 0.
  - `handleSave`: `supabase.from('beam_sale_prices').update(payload).eq('id', prices.id).select().single()`
  - Pola UI: „Domyślna cena sprzedaży [PLN/t]" (opis: „Pre-fill w kalkulatorze — 3800 wg ustaleń"), „Domyślna cena kosztu [PLN/t]" (opis: „0 = kalkulator zostawia pole kosztu puste"), „Notatka".
  - Usuń: cennik szkód, podgląd struktury tygodniowej.

- [ ] **Step 2: Build** → `✓ built in`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sale/beam/BeamSalePriceSettings.tsx
git commit -m "feat(beam-sale): zakladka Cennik — domyslna cena sprzedazy/kosztu (Task 5)"
```

---

## Task 6: `BeamSaveSaleOfferModal.tsx`

**Files:**
- Create: `src/components/sale/beam/BeamSaveSaleOfferModal.tsx`

- [ ] **Step 1: Sklonuj `src/components/sale/SaveSaleOfferModal.tsx`** z modyfikacjami:
  - Usuń wszystko zamkowe (`LockSnapshot`, `lockItems`, sumy zamków, sekcja podglądu zamków) i magazynowe (`warehouseId`/`warehouseName`).
  - Eksportowany snapshot:

```ts
/** Snapshot jednej pozycji dwuteownika – obliczona przez kalkulator */
export interface BeamSaleItemSnapshot {
  profileId: string;
  profileName: string;
  series: string;
  weightKgPerM: number;
  steelGrade: string;
  quantityPcs: number;
  lengthM: number;
  totalLengthM: number;
  massT: number;
  costPerTon: number;    // w walucie oferty
  sellPerTon: number;    // w walucie oferty
  costTotal: number;     // w walucie oferty
  sellTotal: number;     // w walucie oferty
  sellValueEUR: number;  // zawsze EUR
  sellValuePLN: number;  // zawsze PLN
  marginPct: number;
}
```

  - Totals: `{ totalMassT, totalCostEUR, totalSellEUR, totalSellPLN, overallMarginPct }`.
  - Props jak SaveSaleOfferModal (bez `lockItems`), `isUsed?: boolean` + `taskName?: string` opcjonalne (wzorzec propu opcjonalnego). Kafelek „⚠️ Dwuteowniki używane" (lustro linii 429–446, tekst „Dwuteowniki używane" + opis wariantu PDF).
  - Nagłówek modala: `SH/{year}/XXX – numer nadany automatycznie`; przycisk „Zapisz ofertę SH".

- [ ] **Step 2: `handleSave` — INSERT + rollback**:

```ts
const { data, error: err } = await supabase
  .from('beam_sale_offers')
  .insert({
    offer_number: '',
    client_id: clientId,
    task_name: taskName.trim() || null,
    is_used: isUsed,
    status: 'szkic' as OfferStatus,
    notes: notes.trim() || null,
    valid_days: validDays,
    payment_days: paymentDays,
    prepared_by: preparedBy,
    currency,
    exchange_rate: exchangeRate,
    total_mass_t: totals.totalMassT,
    total_cost_eur: totals.totalCostEUR,
    total_sell_eur: totals.totalSellEUR,
    total_sell_pln: totals.totalSellPLN,
    margin_pct: totals.overallMarginPct,
    delivery_trucks: delivery?.trucks ?? null,
    delivery_cost_per_truck: delivery?.costPerTruck ?? null,
    delivery_cost_total: delivery?.totalCostPLN ?? null,
    delivery_paid_by: delivery?.paidBy ?? null,
    delivery_from: delivery?.from?.trim() || null,
    delivery_to: delivery?.to?.trim() || null,
    delivery_timeline: deliveryTimeline,
    campaign_weeks: deliveryTimeline === 'huta' ? campaignWeeks.trim() : null,
    campaign_delivery_weeks: deliveryTimeline === 'huta' ? campaignDeliveryWeeks.trim() || null : null,
    warehouse_delivery_time: deliveryTimeline === 'magazyn' ? warehouseDeliveryTime : null,
    delivery_terms: deliveryTerms,
    fca_location: deliveryTerms === 'FCA' ? fcaLocation.trim() : null,
  })
  .select('*, client:clients(*)')
  .single();
```

  INSERT pozycji do `beam_sale_offer_items` (map snapshot → kolumny snake_case, `sort_order: idx`). Rollback przy błędzie pozycji — **soft-delete, NIE hard delete** (konwencja beam):

```ts
if (itemsErr) {
  await supabase.from('beam_sale_offers').update({ deleted_at: new Date().toISOString() }).eq('id', savedOffer.id);
  setSaving(false);
  return setError('Błąd zapisu pozycji – oferta anulowana: ' + itemsErr.message);
}
```

- [ ] **Step 3: Build** → `✓ built in` (modal jeszcze nieużywany — to OK, kolejność łańcucha).

- [ ] **Step 4: Commit**

```bash
git add src/components/sale/beam/BeamSaveSaleOfferModal.tsx
git commit -m "feat(beam-sale): modal zapisu oferty SH (Task 6)"
```

---

## Task 7: `BeamSaleCalculator.tsx`

**Files:**
- Create: `src/components/sale/beam/BeamSaleCalculator.tsx`

- [ ] **Step 1: Szkielet + stan.** Props:

```ts
interface Props {
  profiles: BeamProfile[];          // wspólny katalog beam_profiles (z sekcji)
  prices: BeamSalePrices;           // cennik 1-wierszowy
  clients: Client[];
  onClientAdded: (c: Client) => void;
  onOfferSaved: (offer: BeamSaleOffer) => void;
}
```

Pozycja kalkulatora (połączenie `CalcItem` z BeamCalculator 18–24 i cen z SaleCalcItem):

```ts
interface BeamSaleCalcItem {
  uid: string;
  profileId: string;
  steelGrade: string;
  quantityPcs: number | '';
  lengthM: number | '';
  costPerTon: number;   // w walucie widoku
  sellPerTon: number;   // w walucie widoku
}
```

Stan: `items`, `currency` start **'PLN'**, kurs NBP przez `fetchNBPRate` (wzorzec SaleCalculator 93–135: `nbpRate`, `manualRate`, `handleManualRateChange`, `loadNBP`), `applyAllSellPrice`, dostawa (`deliveryCostPerTruck`, `customDeliveryTrucks`, `deliveryPaidBy: 'dap_included'|'dap_extra'|'fca'|'cif'`, `deliveryFrom = 'Magazyn Intra B.V.'`, `deliveryTo`), `taskName`, `isUsed`, `showSaveModal`.

Pre-fill cen z cennika (helper):

```ts
// Domyślne ceny z cennika przeliczone do bieżącej waluty widoku.
// PLN kanoniczne; EUR = /exchangeRate zaokrąglone do pełnych (konwencja 'whole').
function defaultPricesInCurrency(cur: 'EUR' | 'PLN', rate: number) {
  const conv = (pln: number) => pln <= 0 ? 0 : (cur === 'PLN' ? pln : Math.round((pln / rate) * 100) / 100);
  return {
    sell: conv(prices.default_sell_price_per_ton_pln),
    cost: conv(prices.default_cost_price_per_ton_pln),
  };
}
```

`addItem()` i pozycja startowa w `useEffect` po załadowaniu: `sellPerTon = defaults.sell`, `costPerTon = defaults.cost` (0 → pole puste jak SP `value={item.costPerTon || ''}`).

- [ ] **Step 2: Obliczenia.** `itemResults` — masa jak BeamCalculator 127–140 ale z zaokrągleniem 3dp (gotcha massT):

```ts
const massT = Math.round((totalLengthM * profile.weight_kg_per_m) / 1000 * 1000) / 1000;
const priceScale = currency === 'EUR' ? 1 : 1 / exchangeRate;
const costEUR = massT * (item.costPerTon || 0) * priceScale;
const sellEUR = massT * (item.sellPerTon || 0) * priceScale;
const marginPct = sellEUR > 0 ? ((sellEUR - costEUR) / sellEUR) * 100 : 0;
```

`totals` = lustro SaleCalculator 343–358 bez wallArea. Walidacja: `hasAllSellPrices = items.every(i => i.sellPerTon > 0)`, `allItemsValid`, `canSave` (lustro 360–368 bez zamków). `deliveryCalc` = lustro 374–386 (masa = totalMassT belek). `handleCurrencyChange` = lustro 397–411 (konwertuj `costPerTon`, `sellPerTon`, `applyAllSellPrice`, `deliveryCostPerTruck` przez `convertCurrencyValue(..., 'whole')`).

- [ ] **Step 3: UI.** Sekcje w kolejności:
  1. Kurs i waluta + „Zastosuj cenę do wszystkich" (lustro SaleCalculator 430–503)
  2. „Pozycje wyceny" — wiersz 1: Profil (select `{p.name} ({p.series})` + hint kg/m, wzorzec BeamCalculator 237–251) | Gatunek (`BEAM_STEEL_GRADES`) | Ilość [szt.] | Długość [m] | Masa [t] (readonly) | Usuń; wiersz 2: Cena kosztu (z linkiem „przywróć" gdy ≠ cennik, wzorzec SaleCalculator 613–632) | Cena sprzedaży (bold, blue) | Marża badge | mini-wyniki
  3. Podsumowanie pozycji (wartość sprzedaży / koszt / masa — lustro 686–715)
  4. Koszt vs Sprzedaż vs Marża (lustro 949–1072, bez zamków)
  5. Koszty dostawy — 4 kafelki + pola + trasa + podsumowanie (lustro 1080–1255; pola kosztów ukryte dla `fca`/`cif`)
  6. Nazwa zadania (input, maxLength 35)
  7. Kafelek „⚠️ Dwuteowniki używane" (lustro 1257–1275, tekst dostosowany)
  8. Walidacja + przycisk „💾 Zapisz jako ofertę SH"
  9. Modal: `BeamSaveSaleOfferModal` z snapshot (lustro 1295–1366 bez zamków — mapowanie `itemResults` → `BeamSaleItemSnapshot`; `sellValueEUR = r.sellEUR`, `sellValuePLN = r.sellEUR * exchangeRate`)

- [ ] **Step 4: Build** → `✓ built in`.

- [ ] **Step 5: Commit**

```bash
git add src/components/sale/beam/BeamSaleCalculator.tsx
git commit -m "feat(beam-sale): kalkulator sprzedazy dwuteownikow (Task 7)"
```

---

## Task 8: `BeamSaleEditOfferModal.tsx` (edycja + kopia, saga)

**Files:**
- Create: `src/components/sale/beam/BeamSaleEditOfferModal.tsx`

- [ ] **Step 1: Sklonuj `src/components/sale/roadplate/RoadPlateEditOfferModal.tsx`** z modyfikacjami:
  - Props: `{ offer: BeamSaleOffer; profiles: BeamProfile[]; prices: BeamSalePrices; clients: Client[]; mode?: 'edit' | 'copy'; onSaved: ...; onClose: ... }`.
  - Pozycje z `offer.items` → stan edytowalny (profil/gatunek/szt/dł/ceny). Pola liczbowe `number | ''`.
  - Czytaj `offer.delivery_paid_by`/`delivery_terms` przez rzutowanie `as` (gotcha poszerzania typu useState).
  - Checkbox `is_used` (jak EditSaleOfferModal — commit `264645b`).
  - Wspólny `const offerPayload = {...}` (wszystkie pola UPDATE bez offer_number/year/sequence) używany przez `.update(offerPayload)` ORAZ copy `.insert({...offerPayload, offer_number: '', status: 'szkic', deleted_at: null})`.
  - **Saga edycji**: (1) UPDATE `beam_sale_offers`; (2) DELETE `beam_sale_offer_items WHERE offer_id`; (3) INSERT nowych pozycji. Przy błędzie kroku 3 komunikat: „stare pozycje zostały usunięte — otwórz edycję ponownie aby naprawić".
  - **Kopia**: INSERT nagłówka (trigger nada numer SH) → INSERT pozycji z `offer_id` nowej oferty; rollback przy błędzie pozycji = `UPDATE deleted_at = now()` świeżej oferty.

- [ ] **Step 2: Build** → `✓ built in`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sale/beam/BeamSaleEditOfferModal.tsx
git commit -m "feat(beam-sale): edycja + kopia oferty SH (saga) (Task 8)"
```

---

## Task 9: `BeamSaleOffersTable.tsx`

**Files:**
- Create: `src/components/sale/beam/BeamSaleOffersTable.tsx`

- [ ] **Step 1: Sklonuj `src/components/sale/roadplate/RoadPlateSaleOffersTable.tsx`** z modyfikacjami:
  - Props: `{ offers, onOffersChange, profiles, prices, clients }` (typy beam-sale).
  - Kolumny listy: numer SH, data, klient, nazwa zadania, masa, wartość sprzedaży (waluta oferty), marża %, status (select inline → UPDATE status), akcje.
  - Expandable rows: pozycje (profil/seria, gatunek, szt × dł, masa, cena sprzedaży/t, wartość) + dostawa + notatki.
  - PDF: `PDFDownloadLink` × 2 (PL: `<BeamSaleOfferPDF offer={o} lang="pl"/>`, EN: `lang="en"`), nazwa pliku `Oferta_{nr}_PL.pdf` (nr z `/` → `_`).
  - Przyciski: „Edytuj" (`editOffer` state → modal `mode="edit"`), zielony „Kopiuj" (`copyOffer` state → drugi render modalu `mode="copy"`, handler **prepend**: `onOffersChange([nowa, ...offers])`), „Usuń" = soft-delete `UPDATE beam_sale_offers SET deleted_at = now()` + confirm.
  - Badge `is_used` (bursztynowy „używane") przy numerze oferty.

- [ ] **Step 2: Build** → `✓ built in`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sale/beam/BeamSaleOffersTable.tsx
git commit -m "feat(beam-sale): lista ofert SH — PDF/edycja/kopia/soft-delete (Task 9)"
```

---

## Task 10: `BeamSaleSection.tsx` (kontener)

**Files:**
- Create: `src/components/sale/beam/BeamSaleSection.tsx`

- [ ] **Step 1: Sklonuj `src/components/beam/BeamRentalSection.tsx`** (bliźniak strukturalny — self-load + obsługa braku migracji) z modyfikacjami:

```ts
export type BeamSaleTab = 'calculator' | 'offers' | 'clients' | 'prices';
```

  - Self-load: `beam_profiles` (`eq('active', true).order('series').order('name')`), `beam_sale_prices` (`.single()`), `beam_sale_offers` (`select('*, client:clients(*), items:beam_sale_offer_items(*)').is('deleted_at', null).order('created_at', {ascending: false})`).
  - Komunikat braku migracji: `docs/migrations/2026-07-10-beam-sale.sql` (kody PGRST205/42P01 — lustro BeamRentalSection 51–64).
  - `handleOfferSaved` → prepend + `onTabChange('offers')`.
  - Render zakładek: calculator (`BeamSaleCalculator`), offers (`BeamSaleOffersTable`), clients (`ClientsTable` — import `'../../ClientsTable'`), prices (`BeamSalePriceSettings`).
  - Komunikat braku profili: „Dodaj przynajmniej jeden profil w zakładce Wynajem → Dwuteowniki → Profile dwuteowników." (katalog wspólny!).

- [ ] **Step 2: Build** → `✓ built in`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sale/beam/BeamSaleSection.tsx
git commit -m "feat(beam-sale): kontener BeamSaleSection — 4 zakladki, self-load (Task 10)"
```

---

## Task 11: Nawigacja w `App.tsx`

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Zmiany punktowe** (wzorzec: jak wpinano `beam` do rental — commit `229b1fb`):
  1. Import: `import BeamSaleSection from './components/sale/beam/BeamSaleSection'; import type { BeamSaleTab } from './components/sale/beam/BeamSaleSection';`
  2. Linia 22: `type SaleSubMode = 'sheet_pile' | 'pipe' | 'road_plate' | 'beam';`
  3. Stany: `const [beamSaleActiveTab, setBeamSaleActiveTab] = useState<BeamSaleTab>('calculator');` + `const [beamSaleOffersCount, setBeamSaleOffersCount] = useState(0);`
  4. Tablica zakładek (po `roadPlateSaleTabs`):

```ts
// Sprzedaż dwuteowników — kalkulator, oferty SH, klienci, cennik.
// Profile to wspólny katalog z wynajmem — edycja w Wynajem → Dwuteowniki.
const beamSaleTabs: { id: BeamSaleTab; label: string; badge?: number }[] = [
  { id: 'calculator', label: 'Kalkulator' },
  { id: 'offers',     label: 'Oferty SH',  badge: beamSaleOffersCount || undefined },
  { id: 'clients',    label: 'Klienci',    badge: clients.length      || undefined },
  { id: 'prices',     label: 'Cennik' },
];
```

  5. `currentTabs` / `currentActiveTab`: gałąź `saleSubMode === 'beam' ? beamSaleTabs/beamSaleActiveTab : ...`
  6. `onClick` nawigacji: `else if (saleSubMode === 'beam') setBeamSaleActiveTab(tab.id as BeamSaleTab);`
  7. Nagłówek (linia ~194): gałąź `saleSubMode === 'beam' ? 'Kalkulator Sprzedaży Dwuteowników' : ...`
  8. Sub-toggle sprzedaży: 4. przycisk „Dwuteowniki" (lustro przycisku rental-beam, linie 258–267).
  9. Main content: gałąź `saleSubMode === 'beam'` → `<BeamSaleSection clients={clients} onClientAdded={...} onClientsChange={setClients} activeTab={beamSaleActiveTab} onTabChange={setBeamSaleActiveTab} onOffersCountChange={setBeamSaleOffersCount} />`

- [ ] **Step 2: Build** → `✓ built in`.

- [ ] **Step 3: Commit**

```bash
git add src/App.tsx
git commit -m "feat(beam-sale): wpiecie modulu SH w nawigacje App (Task 11)"
```

---

## Task 12: Weryfikacja E2E + korekty

**Files:** żadnych nowych (ewentualne fixy w plikach modułu)

- [ ] **Step 1: Uruchom preview** — `preview_start` (konfiguracja `dev`, port 5173; launch.json w `C:\Users\doman\CENNIK\.claude\launch.json`). Gotcha: zombie vite na 5173 → `Stop-Process` przed startem.

- [ ] **Step 2: Przepływ kalkulatora** (stan SPA resetuje się między `preview_eval` — cały przepływ w JEDNYM async evalu; etykiety czytaj przez `textContent`):
  - Sprzedaż → Dwuteowniki → Kalkulator: pozycja HEB300, S355, 10 szt × 12 m → masa oczekiwana **14,4 t** (10×12×120/1000); cena sprzedaży pre-fill **3800**; wartość = 14,4 × 3800 = **54 720 PLN**.
  - Wpisz koszt 3000 → marża ≈ **21,1%**.
  - Dostawa DAP w cenie: 1 auto (14,4 t < 24,5), koszt 2500 PLN → „Łącznie dla klienta" = **57 220 PLN**.

- [ ] **Step 3: Smoke walutowy** — toggle PLN → EUR: ceny pozycji przeliczone `'whole'`; wpisz transport 600 EUR → toggle PLN → oczekiwane **2551** (wzorzec z docs/CURRENCY-CONVERSION-PATTERN.md).

- [ ] **Step 4: Zapis oferty** — modal: klient testowy, zapis → oferta **SH/2026/001**, przejście na listę. Sprawdź w DB (`execute_sql`): nagłówek + pozycje + `is_used = false`.

- [ ] **Step 5: Edycja / kopia / soft-delete** — edytuj (zmień ilość → totale przeliczone), skopiuj (nowy numer SH, status szkic, prepend na liście), usuń kopię (znika z listy; w DB `deleted_at IS NOT NULL`).

- [ ] **Step 6: PDF PL + EN** — pobierz oba dla oferty; sprawdź: 7 kolumn, warunki techniczne (5 punktów z EN 10034, tolerancja -50/+50), blok transportu, kwota dla klienta. Zaznacz „używane" w edycji → PDF z sekcją „Dwuteowniki używane:" bez normy/certyfikatu. Fallback weryfikacji tekstu: `pdfplumber` (gotcha: Read na PDF bywa blokowany przez pdftoppm).

- [ ] **Step 7: Regresja pozostałych modułów** — przeklikaj nagłówki: Wynajem (grodzice/płyty/dwuteowniki), Sprzedaż (grodzice/płyty/rury) — każdy kalkulator się renderuje, liczniki ofert bez zmian.

- [ ] **Step 8: Sprzątanie danych testowych** (zasada: nie zużywać numerów na testy — ale E2E wymaga realnego zapisu):

```sql
DELETE FROM beam_sale_offers WHERE year = 2026;  -- CASCADE usunie pozycje
UPDATE beam_sale_sequences SET last_sequence = 0 WHERE year = 2026;
```

- [ ] **Step 9: Commit końcowy (fixy z E2E, jeśli były)**

```bash
git add -A
git commit -m "fix(beam-sale): korekty po weryfikacji E2E (Task 12)"
```

- [ ] **Step 10: Zapytaj użytkownika o push na main** (auto-deploy Netlify!). Po push: weryfikacja deployu — hash bundla / grep `BEAM_SALE_PDF_STRINGS`-owego literału (np. `OFERTA SPRZEDAŻY DWUTEOWNIKÓW`) w `dist/assets/index-*.js` na Netlify.
